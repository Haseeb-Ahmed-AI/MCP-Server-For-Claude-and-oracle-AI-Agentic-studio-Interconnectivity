# MCP-Server-For-Claude-and-oracle-AI-Agentic-studio-Interconnectivity
MCP server that connects Claude Desktop to Oracle AI Agent Studio, letting users query customer data through natural language instead of the Oracle console. Currently uses mock data for demo purposes; built to swap in a live Agent Studio connection later.
